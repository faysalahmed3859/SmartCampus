@model IEnumerable<SmartCampus.Models.Category>
@{
    ViewData["Title"] = "Default";
}

    @*<ul class="nav navbar-nav">
        @foreach (var item in Model)
        {
           <li class="nav-item">
               <a class="nav-link text-dark" asp-area="Admin" asp-controller="Category" asp-action="GetCategoryDetails" asp-route-id="@item.Id">@item.Name</a>
          </li>
        }
    </ul>*@

<ul class="dropdown-menu">

    @foreach (var item in Model)
    {
        <li>
            <a asp-controller="Home" asp-action="GetAllResourceByCategory" asp-route-categoryId="@item.Id">@item.Name</a>
   
        </li> 
    }
</ul>
